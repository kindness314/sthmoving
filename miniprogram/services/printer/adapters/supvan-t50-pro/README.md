# 硕方 T50 Pro 适配层

此目录只预留真实设备适配位置，不包含模拟成功实现。

取得硕方提供的微信小程序 SDK 后：

1. 将厂商 SDK 按其授权和集成说明放入项目；
2. 在此目录实现 `PrinterAdapter`；
3. 将通用的 `BitmapPrintJob` 转换为 SDK 要求的数据格式；
4. 补充真机连接、断连、缺纸、低电量和打印失败测试；
5. 确认 30 mm × 30 mm 标签在设备分辨率下的有效打印区域。

厂商 SDK 是唯一允许直接调用蓝牙和打印 API 的模块，业务页面仅依赖
`PrinterAdapter`。
