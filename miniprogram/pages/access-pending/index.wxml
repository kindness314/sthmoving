<view class="page pending-page">
  <view class="card">
    <view class="title">{{title}}</view>
    <view class="muted">{{description}}</view>
    <view wx:if="{{canApply}}" class="application-form">
      <view class="field-label">姓名或称呼</view>
      <input
        class="text-input"
        maxlength="40"
        value="{{displayName}}"
        placeholder="便于管理员确认身份"
        bindinput="handleDisplayNameInput"
      />
      <view class="field-label role-label">申请角色</view>
      <radio-group bindchange="handleRequestedRoleChange" class="role-options">
        <label class="role-option">
          <radio value="MEMBER" checked="{{requestedRole === 'MEMBER'}}" />
          <text>普通成员</text>
        </label>
        <label class="role-option">
          <radio value="ADMIN" checked="{{requestedRole === 'ADMIN'}}" />
          <text>管理员</text>
        </label>
      </radio-group>
      <button
        class="primary-button submit-button"
        loading="{{submitting}}"
        disabled="{{submitting || !displayName}}"
        bindtap="handleSubmit"
      >
        提交加入申请
      </button>
    </view>
    <button
      wx:else
      class="retry-button"
      loading="{{loading}}"
      disabled="{{loading}}"
      bindtap="handleRetry"
    >
      刷新状态
    </button>
    <view wx:if="{{errorMessage}}" class="error">{{errorMessage}}</view>
  </view>
</view>
