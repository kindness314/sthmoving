<view class="page">
  <view class="heading">
    <view class="title">仓库工作台</view>
    <view class="muted">
      {{userName ? userName + '，欢迎回来' : '正在确认身份'}}
    </view>
  </view>
  <view class="grid">
    <view
      wx:for="{{shortcuts}}"
      wx:key="key"
      class="card shortcut"
      data-key="{{item.key}}"
      bindtap="handleShortcut"
    >
      <view class="shortcut-title">{{item.title}}</view>
      <view class="muted shortcut-description">{{item.description}}</view>
    </view>
  </view>
</view>
