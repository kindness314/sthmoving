# 共享物品管理微信小程序：实施计划

## 1. 文档目的

本文档把《产品需求与架构》拆解为可以逐阶段开发、验证和交付的任务。

开发时以《产品需求与架构》定义产品规则，以本文档确定实施顺序和阶段验收标准。
任何阶段只有在代码检查、类型检查、自动化测试及对应人工验收均通过后，才进入下一阶段。

## 2. 当前状态

### 2.1 已具备

- GitHub 公有仓库和 `main` 分支；
- 微信原生小程序 + TypeScript 工程结构；
- 登录、待审核、工作台页面骨架；
- 用户、物品、分类、离库、操作日志和标签领域类型；
- 云函数统一 API 入口与路由骨架；
- 小程序码服务调用边界；
- `PrinterAdapter`、30 mm × 30 mm 位图任务和硕方 T50 Pro 适配目录；
- ESLint、TypeScript、Vitest 和 GitHub Actions；
- 4 个测试文件，共 11 项基础单元测试。

### 2.2 尚未具备

- 微信身份登录和首位所有者初始化；
- 成员申请、审核和权限管理；
- 云数据库集合、索引和真实数据读写；
- 分类初始化、物品登记、图片上传和编辑；
- 文字搜索、分类筛选和扫码查询；
- 乐观锁更新、冲突对比和重新提交；
- 操作日志和提交梗概持久化；
- 离库申请、审核和管理员直接离库；
- 管理员站内提醒及微信订阅消息；
- 真实小程序码生成、绑定和作废；
- 硕方 SDK、蓝牙连接及 T50 Pro 真机打印；
- 微信开发者工具和真机端到端验证。

当前云函数除 `system.ping` 外，业务接口会明确返回 `NOT_IMPLEMENTED`。页面上的业务入口也只是骨架，
不能作为可用产品发布。

## 3. 开始开发前需要的资料

### 3.1 立即需要

| 资料 | 提供者 | 用途 | 安全要求 |
| --- | --- | --- | --- |
| 微信小程序 AppID | 项目所有者 | 关联微信开发者工具和真实小程序 | 可以写入公共项目配置 |
| 微信云开发环境 ID | 项目所有者 | 初始化云函数、数据库和云存储 | 可以写入环境配置，但建议区分公开默认值和本机私有值 |
| 组织名称 | 项目所有者 | 登录页、申请页和通知文案 | 无特殊要求 |
| 首位所有者人选 | 项目所有者 | 建立第一个 `OWNER` 账号 | 只确认人员，不在仓库中保存敏感身份数据 |

### 3.2 后续阶段需要

| 资料 | 最晚需要阶段 | 用途 |
| --- | --- | --- |
| 小程序头像、名称和基础视觉偏好 | 阶段 2 | 页面标题、空状态和发布资料 |
| 微信订阅消息模板及模板 ID | 阶段 6 | 新成员申请和离库申请提醒 |
| 硕方 T50 Pro 微信小程序 SDK | 阶段 7 | 真机连接和打印 |
| T50 Pro 实机及 30 mm × 30 mm 标签纸 | 阶段 7 | 打印尺寸、清晰度和扫码验证 |
| 至少一名管理员和一名成员测试账号 | 阶段 6 | 权限和审批流程真机验收 |

### 3.3 不应提供或提交

- 小程序 AppSecret；
- 微信 access token；
- 云开发密钥；
- 硕方 SDK 的授权密钥；
- 用户真实 OpenID 清单；
- 任何只应保存在微信平台、云环境变量或本机私有配置中的凭证。

## 4. 总体实施顺序

```text
阶段 0 环境接通
  → 阶段 1 登录与成员体系
  → 阶段 2 分类、物品和图片
  → 阶段 3 查询、小程序码和标签
  → 阶段 4 编辑冲突与操作日志
  → 阶段 5 离库流程
  → 阶段 6 管理员提醒
  → 阶段 7 T50 Pro 打印
  → 阶段 8 系统验收与发布
```

阶段 1 至阶段 6 构成可上线的核心业务。阶段 7 依赖购买设备后才能取得的硕方 SDK，可以在不阻塞其他
业务的情况下最后接入。

## 5. 阶段 0：接通微信环境

### 5.1 目标

让当前工程可以在微信开发者工具中编译、调用云函数并识别当前微信用户。

### 5.2 开发任务

1. 将真实 AppID 写入微信项目配置；
2. 建立本机私有配置，避免提交开发者工具个人设置；
3. 配置云开发环境 ID；
4. 在微信开发者工具中启用云开发；
5. 创建并部署 `api` 云函数；
6. 调用 `system.ping` 验证小程序到云函数的链路；
7. 确认服务端可以取得 `OPENID`，但不在日志或页面中直接暴露；
8. 明确开发环境与正式环境的配置方式；
9. 写入环境配置和部署操作说明。

### 5.3 验收标准

- 微信开发者工具可以无错误编译小程序；
- 真机或开发者工具可以成功调用 `system.ping`；
- 云函数能够取得微信身份上下文；
- 公有仓库不包含 AppSecret、密钥或真实 OpenID；
- `npm run check` 和 `npm run build:cloud` 通过。

### 5.4 用户需要配合

- 提供 AppID 和云环境 ID；
- 用目标微信账号运行一次小程序，以确定首位所有者；
- 确认组织名称。

## 6. 阶段 1：登录、加入申请与权限

### 6.1 目标

形成“微信登录—申请加入—管理员审核—获得成员权限”的第一个完整业务闭环。

### 6.2 数据结构

- `users`：微信身份映射、昵称、头像、角色、账号状态和时间；
- `join_requests`：申请人、申请状态、审核人、审核意见和时间；
- 必要索引：
  - `users.openid` 唯一；
  - `join_requests.applicant_id + status`；
  - `join_requests.status + created_at`。

### 6.3 服务端任务

1. 实现 `auth.login`：
   - 只从云函数上下文读取 OpenID；
   - 返回已有用户状态；
   - 未加入用户进入申请流程；
2. 实现首位所有者初始化：
   - 使用一次性的受控初始化流程；
   - 初始化成功后关闭入口；
   - 不允许客户端自行声明 `OWNER`；
3. 实现加入申请创建；
4. 允许被拒绝用户重新申请，但同一用户不能同时存在多个待审核申请；
5. 实现管理员申请列表；
6. 实现通过和拒绝；
7. 在每个受保护 API 上校验用户为 `ACTIVE`；
8. 服务端统一执行角色权限检查，不能只依赖页面隐藏按钮。

### 6.4 小程序端任务

1. 完成微信登录页；
2. 展示未申请、待审核、已拒绝、已禁用四种状态；
3. 被拒绝后显示重新申请入口；
4. 建立管理员申请列表和审核页面；
5. 根据角色展示成员功能和管理功能；
6. 处理会话失效及重新进入后的状态刷新。

### 6.5 测试重点

- 新用户首次登录；
- 重复登录不会创建重复用户；
- 同一用户不能重复提交待审核申请；
- 被拒绝用户可以再次申请；
- 普通成员不能调用审核接口；
- 管理员不能将自己提升为所有者；
- 非激活和已禁用用户不能访问物品数据；
- 客户端伪造用户 ID 或角色无效。

### 6.6 验收标准

- 所有登录与申请状态均有明确页面；
- 管理员能够通过或拒绝申请；
- 被拒绝用户能够再次申请；
- 权限绕过测试失败，即服务端正确拒绝越权调用；
- 自动化测试和两种角色的真机流程通过。

## 7. 阶段 2：分类、物品登记与图片

### 7.1 目标

成员可以创建分类并完整登记物品，任何激活成员都能查看物品。

### 7.2 分类任务

1. 创建 `categories` 集合；
2. 初始化以下预设分类：
   - 日常用品；
   - 清洁用品；
   - 技术设备；
   - 电脑配件；
   - 装饰品；
   - 招新用品；
   - 纸质材料；
   - 待认领物品；
   - 个人存放物品；
3. 预设分类初始化必须具备幂等性，重复执行不能生成重复数据；
4. 允许成员新建分类；
5. 分类名称去除首尾空格后不能重复；
6. 本阶段不提供分类删除功能。

### 7.3 物品登记任务

1. 创建 `items` 集合及查询索引；
2. 实现物品表单：
   - 图片最多两张；
   - 名称；
   - 详情；
   - 单件或多件模式；
   - 多件模式下的具体数量；
   - 预设或自定义分类；
   - 提交梗概；
3. 单件模式的数量固定为 `1`；
4. 多件模式的数量必须是大于 `0` 的整数；
5. 服务端生成物品 ID、标签编码、版本号和时间；
6. 写入 `created_by`、`updated_by`，初始版本为 `1`；
7. 登记物品和首条操作日志在同一事务中完成。

### 7.4 图片任务

1. 在实现前重新核对微信当前图片选择、压缩、上传和云存储限制；
2. 客户端在上传前检查图片数量和实际文件大小；
3. 图片上传至云存储，数据库只保存云文件 ID；
4. 物品写入失败时不得让失败状态显示为登记成功；
5. 编辑替换图片时，在新版本成功写入后再处理不再引用的旧文件；
6. 列表使用适合列表展示的图片尺寸，详情页再加载完整图片。

### 7.5 操作日志

创建物品时必须同时写入一条操作日志：

- 动作：`CREATE`；
- 操作人；
- 操作时间；
- 提交梗概；
- 物品版本 `1`。

日志不保存或展示字段修改的具体内容。

### 7.6 验收标准

- 预设分类只初始化一次；
- 成员可以新建分类并立即用于登记；
- 单件、多件及数量校验符合规则；
- 最多上传两张图片；
- 新物品包含登记人、最后修改人、日期和版本；
- 创建成功必然存在对应日志；
- 失败请求不会产生半条物品记录或错误成功提示。

## 8. 阶段 3：物品查询、小程序码与实体标签

### 8.1 目标

用户可以通过文字、分类或实体小程序码快速定位物品。

### 8.2 文字与分类查询

1. 建立物品列表页；
2. 支持按名称和详情搜索；
3. 支持按分类筛选；
4. 默认只展示 `IN_STOCK` 物品；
5. 明确排序规则，默认按最后更新时间倒序；
6. 使用分页加载，避免一次读取全部物品；
7. 根据最终查询方式建立数据库索引；
8. 增加无结果、加载中、加载失败和无权限状态。

### 8.3 物品详情

详情页展示：

- 最多两张图片；
- 名称和详情；
- 数量模式及数量；
- 分类；
- 登记日期；
- 最后更新日期；
- 登记人；
- 最后修改人；
- 当前版本；
- 操作日志入口；
- 编辑入口；
- 离库申请入口。

### 8.4 小程序码

1. 标签使用随机、不可猜测且不包含业务信息的 `label_code`；
2. 服务端调用微信小程序码接口；
3. 小程序码参数只携带标签编码，不携带名称、数量等可变信息；
4. 扫码进入详情页后，服务端根据标签编码查询当前物品；
5. 小程序码图片保存至云存储；
6. `item_labels` 记录标签与物品的绑定关系和状态；
7. 已离库或无效标签返回明确状态，不跳转到其他物品；
8. 小程序码生成失败时允许管理员重新生成，但不能产生多个同时有效的绑定。

### 8.5 30 mm × 30 mm 标签排版

标签图的逻辑内容至少包含：

- 可扫描的小程序码；
- 必要的留白区；
- 可选的简短物品编号；
- 不依赖会频繁变化的名称和数量。

在 T50 Pro 适配前先输出统一的单色位图打印任务。当前按 8 dots/mm 建立 240 × 240 dots 的基准，
最终有效打印区域、二维码边长和留白必须用实机校准。

### 8.6 验收标准

- 名称和详情搜索返回正确物品；
- 分类筛选及分页无重复、无漏项；
- 扫描标签能够打开对应物品详情；
- 小程序码不暴露物品字段或用户信息；
- 已离库和作废标签不能显示为在库；
- 在多种手机尺寸上详情页和扫码流程可用。

## 9. 阶段 4：编辑、版本冲突与操作日志

### 9.1 目标

任何成员都能编辑允许字段，同时避免后提交者无提示覆盖其他人的修改。

### 9.2 可编辑范围

成员可以修改：

- 图片；
- 名称；
- 详情；
- 数量模式；
- 数量。

物品 ID、标签编码、登记人、登记日期、状态和历史日志不能通过普通编辑接口修改。

### 9.3 乐观锁实现

1. 用户打开编辑页时保存基准版本和基准字段；
2. 提交时发送 `expected_version` 和提交梗概；
3. 服务端在事务中判断当前版本是否等于 `expected_version`；
4. 版本一致时：
   - 更新允许字段；
   - 版本号加 `1`；
   - 更新 `updated_by` 和 `updated_at`；
   - 同事务写入操作日志；
5. 版本不一致时返回：
   - 最新物品；
   - 最新版本号；
   - 冲突错误码；
6. 客户端使用“打开时版本、自己的草稿、服务端最新版”做三方对比；
7. 只突出显示发生变化的字段；
8. 用户基于最新版确认内容并重新提交，系统不能自动覆盖。

### 9.4 提交梗概

- 新建和编辑必须填写；
- 去除首尾空格后长度为 5–100 个字符；
- 列表中展示梗概、操作人、时间、动作和版本；
- 日志不展示图片、名称、详情或数量的前后值；
- 日志只能读取，不能编辑或删除。

### 9.5 测试重点

- 两名成员同时打开同一版本；
- 第一人提交后版本加一；
- 第二人提交收到冲突而非成功；
- 对比页准确标出双方变化；
- 第二人基于最新版重新提交成功；
- 更新和日志写入必须同成同败；
- 提交梗概为空、过短或过长时拒绝。

### 9.6 验收标准

- 不会出现无提示覆盖；
- 冲突页能清楚区分最新版与本地草稿；
- 每次成功修改只生成一条日志；
- 日志只包含规定的审计信息；
- 并发测试、事务测试和页面人工测试通过。

## 10. 阶段 5：离库申请与管理员操作

### 10.1 目标

成员不能直接删除物品，只能申请离库；管理员可以审批或直接离库。

### 10.2 成员流程

1. 在在库物品详情页提交离库申请；
2. 必须填写离库原因；
3. 同一物品存在待审核申请时，不允许重复申请；
4. 成员可以查看自己的申请状态；
5. 被拒绝后物品保持在库，并允许再次申请。

### 10.3 管理员流程

1. 查看全部待审核离库申请；
2. 查看物品当前信息、申请人、原因和申请时间；
3. 同意时在事务中：
   - 检查物品仍在库；
   - 将物品状态改为 `OUTBOUND`；
   - 记录审核人和时间；
   - 写入离库操作日志；
   - 将标签状态改为 `VOID`；
4. 拒绝时记录审核人、审核意见和时间，物品保持在库；
5. 管理员可直接离库，但必须填写提交梗概或离库原因；
6. 已离库物品不提供恢复入库；
7. 任何客户端都不能物理删除物品及日志记录。

### 10.4 并发和异常

- 两名管理员同时审核时，只允许一个请求改变最终状态；
- 审核期间物品已离库时，后续请求返回明确冲突；
- 已离库物品不能再次申请离库；
- 标签、物品、申请和日志状态必须保持一致。

### 10.5 验收标准

- 成员无法直接调用离库接口；
- 管理员能够同意、拒绝和直接离库；
- 拒绝后可以重新申请；
- 已离库物品不出现在默认在库列表；
- 扫描已作废标签显示已离库状态；
- 不存在恢复入口或物理删除入口。

## 11. 阶段 6：管理员提醒

### 11.1 目标

管理员能够及时看到新的成员申请和离库申请。

### 11.2 站内提醒

1. 创建 `notifications` 集合；
2. 新成员申请时给所有激活管理员和所有者创建提醒；
3. 新离库申请时创建对应提醒；
4. 提供未读数量；
5. 提供提醒列表和已读操作；
6. 点击提醒进入对应审核详情；
7. 申请已被其他管理员处理时，详情展示最终状态。

### 11.3 微信订阅消息

1. 在微信公众平台申请合适的订阅消息模板；
2. 保存模板 ID 到服务端环境配置；
3. 仅在用户明确授权后发送；
4. 消息内容只包含审核所需的最少信息；
5. 发送失败不改变申请创建或审核结果；
6. 记录发送结果用于排查，但不向普通用户展示内部错误。

订阅消息受微信平台模板、授权次数和发送规则约束，实施时必须以当时的官方规则为准重新查证。

### 11.4 验收标准

- 新申请必然出现在管理员站内提醒；
- 未读数、已读状态和跳转正确；
- 多名管理员都能收到相应提醒；
- 订阅消息只在已授权且满足平台规则时发送；
- 消息失败不会导致业务事务回滚或重复创建申请。

## 12. 阶段 7：硕方 T50 Pro 真机打印

### 12.1 前置条件

- 已取得硕方提供的微信小程序 SDK、文档和示例；
- 已有 T50 Pro 实机；
- 已有 30 mm × 30 mm 标签纸；
- 已确认 SDK 的授权、打包和发布限制。

### 12.2 集成原则

- 厂商 API 只允许出现在
  `miniprogram/services/printer/adapters/supvan-t50-pro/`；
- 页面和标签业务只依赖 `PrinterAdapter`；
- 不修改通用打印任务去适配某个页面；
- 不添加“打印成功”的模拟兜底；
- SDK 错误必须转换为稳定的业务错误状态。

### 12.3 开发任务

1. 核对 SDK 支持的蓝牙协议、小程序基础库版本和设备权限；
2. 实现设备发现；
3. 实现连接、断开和连接状态查询；
4. 将统一单色位图转换为 SDK 数据；
5. 实现份数、打印进度和完成回调；
6. 处理蓝牙关闭、连接超时、设备断开、缺纸、低电量和打印失败；
7. 页面提供重新连接和重新打印入口；
8. 记录打印任务状态，但不能把调用 SDK 成功等同于标签已贴到物品；
9. 校准二维码尺寸、纠错等级、留白和打印浓度；
10. 验证连续打印和重复打印不会改变标签绑定关系。

### 12.4 真机测试

- iOS 和 Android 各至少一台设备；
- 首次授权与拒绝授权；
- 蓝牙关闭后开启；
- 搜索多个附近设备；
- 打印中断；
- 缺纸后恢复；
- 同一标签打印一份及多份；
- 连续打印多张不同标签；
- 弱光、倾斜、轻微污损和不同距离扫码；
- 标签贴到物品后的实际扫码速度。

### 12.5 验收标准

- 能稳定发现并连接 T50 Pro；
- 30 mm × 30 mm 标签内容不裁切；
- 常用手机微信可以快速识别小程序码；
- 设备错误有明确提示且不会显示虚假成功；
- 重新打印不会创建新的物品或错误标签绑定；
- 硕方 SDK 更新影响被限制在适配目录内。

## 13. 阶段 8：系统验收与发布

### 13.1 自动化检查

每次合并前执行：

```bash
npm run check
npm run build:cloud
```

检查内容至少包括：

- ESLint；
- 小程序、云函数和测试 TypeScript 类型检查；
- 权限、输入、路由、并发、日志、离库和打印任务测试；
- 云函数编译。

### 13.2 人工验收角色

至少使用：

- 1 个所有者账号；
- 1 个管理员账号；
- 2 个成员账号；
- 1 个未加入或被拒绝账号。

两名成员用于真实验证并发编辑，两名管理权限账号用于验证并发审核。

### 13.3 核心回归流程

1. 新用户登录并申请加入；
2. 管理员收到提醒并审核；
3. 成员新建分类并登记带图片物品；
4. 通过文字和扫码找到物品；
5. 两名成员同时编辑并触发冲突；
6. 查看操作日志；
7. 成员申请离库；
8. 管理员拒绝后成员再次申请；
9. 管理员同意离库；
10. 扫描原标签确认物品已离库；
11. 使用 T50 Pro 打印并扫描实体标签。

### 13.4 发布准备

1. 设置正式 AppID 和云环境；
2. 复核数据库和云存储权限；
3. 配置合法域名及微信平台所需资料；
4. 检查隐私政策和用户信息使用说明；
5. 清理测试账号和测试数据；
6. 建立数据备份与日志保留策略；
7. 构建并上传体验版；
8. 完成多账号真机验收；
9. 提交微信审核；
10. 审核通过后发布并观察首批真实操作。

### 13.5 最终完成标准

- 《产品需求与架构》中的 MVP 验收用例全部通过；
- 所有角色权限由服务端强制执行；
- 核心写操作具备事务或等价的一致性保障；
- 并发编辑不会无提示覆盖；
- 物品日志不可篡改且信息符合约定；
- 离库后不可恢复，标签状态一致；
- 管理员能够收到两类申请提醒；
- 实体标签打印和扫码通过真机测试；
- 仓库、云环境和微信平台中均不存在泄露凭证；
- 本地检查和 GitHub CI 均通过。

## 14. 每阶段交付规范

每个阶段交付时必须说明：

1. 本阶段实现了哪些用户可见功能；
2. 修改了哪些数据结构和接口；
3. 执行了哪些自动化检查；
4. 完成了哪些人工或真机测试；
5. 哪些内容因为外部资料或设备尚未验证；
6. 是否存在数据库迁移、云函数部署或平台配置步骤；
7. 下一阶段开始前还需要项目所有者提供什么。

没有执行的检查必须明确标记为“未验证”，不得写成“通过”。

## 15. 推荐的近期执行批次

### 批次 A：环境与身份闭环

包含阶段 0 和阶段 1。完成后可以登录、申请加入和审核成员，是后续所有功能的权限基础。

开始条件：

- AppID；
- 云环境 ID；
- 组织名称；
- 首位所有者人选。

### 批次 B：物品最小闭环

包含阶段 2 和阶段 3。完成后可以登记、查看、搜索、扫码和识别物品。

开始条件：

- 批次 A 验收通过；
- 确认小程序基础视觉信息；
- 云存储可用。

### 批次 C：协作与离库闭环

包含阶段 4、阶段 5 和站内提醒。完成后可以安全地多人编辑、审计和审批离库。

开始条件：

- 至少两名成员测试账号；
- 至少两名具有审核权限的测试账号。

### 批次 D：外部能力与发布

包含微信订阅消息、阶段 7 和阶段 8。

开始条件：

- 微信订阅消息模板；
- 硕方 SDK；
- T50 Pro 实机和标签纸；
- 微信发布所需资料。

## 16. 当前下一步

下一步执行“批次 A：环境与身份闭环”。

项目所有者提供 AppID、云环境 ID、组织名称和首位所有者人选后，开发从阶段 0 开始。
在这些信息到位前，不应继续猜测微信环境配置或实现依赖真实身份的业务逻辑。
