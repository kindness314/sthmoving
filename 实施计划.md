# 共享物品管理微信小程序：实施计划

## 1. 文档目的

本文档把《产品需求与架构》拆解为可以逐阶段开发、验证和交付的任务。

开发时以《产品需求与架构》定义产品规则，以本文档确定实施顺序和阶段验收标准。
任何阶段只有在代码检查、类型检查、自动化测试及对应人工验收均通过后，才进入下一阶段。

## 2. 当前状态

### 2.1 已具备

- GitHub 公有仓库和 `main` 分支；
- 微信原生小程序 + TypeScript 工程结构和云函数统一 API；
- 微信登录、首位所有者初始化、成员申请与管理员审核；
- 分类初始化、自定义分类和管理员分类管理；
- 物品登记、最多两张图片上传和首条操作日志；
- 物品列表、文字搜索、分类筛选、游标分页和物品详情；
- 小程序码生成、云存储、标签预览和扫码路由代码；
- 阶段 3 小程序码、标签绑定和扫码进入详情验收；
- 阶段 4 物品编辑、版本冲突、冲突对比和操作日志验收；
- 阶段 5 成员离库申请、管理员审批/拒绝、直接离库和标签作废；
- 阶段 6 管理员提醒代码和云环境验收；
- `PrinterAdapter`、30 mm × 30 mm 位图任务、硕方 T50 Pro SDK 适配器和标签页打印入口；
- ESLint、TypeScript、Vitest 和 GitHub Actions；
- 数据库集合、权限和已实现功能所需的索引配置文档。

### 2.2 尚未具备

- T50 Pro 已完成至少一台手机的连接、30 mm × 30 mm 标签打印和扫码回查；双平台及连续 20 张压力验收尚未完成；
- 完整系统真机端到端验收（需在微信开发者工具和真机上执行）；
- 首位实际管理者需要由所有者在成员管理页从已通过审核的管理员中手动任命，系统不会猜测或自动指定人员。

阶段 3 的小程序码、云存储、标签绑定和微信扫一扫深链已完成云端及人工验收；阶段 4 的编辑、版本冲突、
操作日志和索引也已完成部署及人工验收；阶段 5 和阶段 6 已由项目所有者完成第六轮人工验收。
当前待处理内容集中在本轮提出的权限层级、审核体验、内置扫码入口和分类信息对齐。

## 3. 开始开发前需要的资料

### 3.1 立即需要

| 资料 | 提供者 | 用途 | 安全要求 |
| --- | --- | --- | --- |
| 微信小程序 AppID | 项目所有者 | 关联微信开发者工具和真实小程序 | 仅写入本机 `project.private.config.json`，不提交到公共仓库 |
| 微信云开发环境 ID | 项目所有者 | 初始化云函数、数据库和云存储 | 可以写入环境配置，但建议区分公开默认值和本机私有值 |
| 组织名称 | 项目所有者 | 登录页、申请页和通知文案 | 无特殊要求 |
| 首位所有者人选 | 项目所有者 | 建立第一个 `OWNER` 账号 | 只确认人员，不在仓库中保存敏感身份数据 |

### 3.2 后续阶段需要

| 资料 | 最晚需要阶段 | 用途 |
| --- | --- | --- |
| 小程序头像、名称和基础视觉偏好 | 阶段 2 | 页面标题、空状态和发布资料 |
| 微信订阅消息模板及模板 ID | 阶段 6（可选） | 外部审核提醒 |
| 硕方 T50 Pro 微信小程序 SDK | 阶段 8 | 已取得并按压缩包 SHA256 固定版本，本体仅保存在本机忽略目录 |
| T50 Pro 实机及 30 mm × 30 mm 标签纸 | 阶段 8 | 打印尺寸、清晰度和扫码验证 |
| 至少一名管理员和一名成员测试账号 | 阶段 6 | 权限和审批流程真机验收 |

### 3.3 不应提供或提交

- 小程序 AppSecret；
- 微信 access token；
- 云开发密钥；
- 硕方 SDK 的授权密钥；
- 用户真实 OpenID 清单；
- 任何只应保存在微信平台、云环境变量或本机私有配置中的凭证。

## 4. 总体实施顺序

```text
阶段 0 环境接通
  → 阶段 1 登录与成员体系
  → 阶段 2 分类、物品和图片
  → 阶段 3 查询、小程序码和标签
  → 阶段 4 编辑冲突与操作日志
  → 阶段 5 离库流程
  → 阶段 6 管理员提醒
  → 阶段 7 权限、审核体验与扫码查询补齐
  → 阶段 8 T50 Pro 打印
  → 阶段 9 系统验收与发布
```

阶段 1 至阶段 7 构成可上线的核心业务。阶段 8 的 SDK 和客户端适配代码已经接入，剩余工作依赖
T50 Pro 实机、30 mm × 30 mm 标签纸以及 iOS/Android 真机验证。

## 5. 阶段 0：接通微信环境

### 5.1 目标

让当前工程可以在微信开发者工具中编译、调用云函数并识别当前微信用户。

### 5.2 开发任务

1. 将真实 AppID 写入本机私有微信项目配置；
2. 建立本机私有配置，避免提交开发者工具个人设置；
3. 配置云开发环境 ID；
4. 在微信开发者工具中启用云开发；
5. 创建并部署 `api` 云函数；
6. 调用 `system.ping` 验证小程序到云函数的链路；
7. 确认服务端可以取得 `OPENID`，但不在日志或页面中直接暴露；
8. 明确开发环境与正式环境的配置方式；
9. 写入环境配置和部署操作说明。

### 5.3 验收标准

- 微信开发者工具可以无错误编译小程序；
- 真机或开发者工具可以成功调用 `system.ping`；
- 云函数能够取得微信身份上下文；
- 公有仓库不包含 AppSecret、密钥或真实 OpenID；
- `npm run check` 和 `npm run build:cloud` 通过。

### 5.4 用户需要配合

- 提供 AppID 和云环境 ID；
- 用目标微信账号运行一次小程序，以确定首位所有者；
- 确认组织名称。

## 6. 阶段 1：登录、加入申请与权限

### 6.1 目标

形成“微信登录—申请加入—管理员审核—获得成员权限”的第一个完整业务闭环。

### 6.2 数据结构

- `users`：微信身份映射、昵称、头像、角色、账号状态和时间；角色包含 `OWNER / MANAGER / ADMIN / MEMBER`；
- `join_requests`：申请人、申请目标角色、最终角色、申请状态、审核人、审核意见和时间；
- 必要索引：
  - `user_openid_unique`：`openid` 升序，唯一；
  - `user_status_role_created_at`：`status` 升序、`role` 升序、`created_at` 降序，非唯一；
  - `join_request_applicant_status`：`applicant_id` 升序、`status` 升序，非唯一；
  - `join_request_status_role_created_at`：`status` 升序、`requested_role` 升序、`created_at` 降序，非唯一。

### 6.3 服务端任务

1. 实现 `auth.login`：
   - 只从云函数上下文读取 OpenID；
   - 返回已有用户状态；
   - 未加入用户进入申请流程；
2. 实现首位所有者初始化：
   - 使用一次性的受控初始化流程；
   - 初始化成功后关闭入口；
   - 不允许客户端自行声明 `OWNER`；
3. 实现加入申请创建，申请人必须选择普通成员或管理员目标角色；
4. 允许被拒绝用户重新申请，但同一用户不能同时存在多个待审核申请；
5. 实现管理员申请列表，明确展示申请目标角色；
6. 实现通过和拒绝，并限制管理员申请只能由实际管理者或所有者批准；
7. 在每个受保护 API 上校验用户为 `APPROVED`；
8. 服务端统一执行角色权限检查，不能只依赖页面隐藏按钮。

### 6.4 小程序端任务

1. 完成微信登录页；
2. 展示未申请、待审核、已拒绝、已禁用四种状态；
3. 被拒绝后显示重新申请入口；
4. 建立管理员申请列表和审核页面；
5. 根据角色展示成员功能和管理功能；
6. 将首页作为普通启动入口，首次进入静默调用登录接口；后续重新进入和回到前台时刷新当前用户与成员权限，不要求重复点击登录按钮；
7. 处理会话失效及重新进入后的状态刷新，登录页只作为失败重试入口。

### 6.5 测试重点

- 新用户首次登录；
- 重复登录不会创建重复用户；
- 同一用户不能重复提交待审核申请；
- 被拒绝用户可以再次申请；
- 普通成员不能调用审核接口；
- 管理员不能将自己提升为所有者；
- 非激活和已禁用用户不能访问物品数据；
- 客户端伪造用户 ID 或角色无效。

### 6.6 验收标准

- 所有登录与申请状态均有明确页面；
- 管理员能够通过或拒绝申请；
- 被拒绝用户能够再次申请；
- 权限绕过测试失败，即服务端正确拒绝越权调用；
- 自动化测试和两种角色的真机流程通过。

## 7. 阶段 2：分类、物品登记与图片

### 7.1 目标

成员可以创建分类并完整登记物品，任何激活成员都能查看物品。

### 7.2 分类任务

1. 创建 `categories` 集合；
2. 初始化以下预设分类：
   - 日常用品；
   - 清洁用品；
   - 技术设备；
   - 电脑配件；
   - 装饰品；
   - 招新用品；
   - 纸质材料；
   - 待认领物品；
   - 个人存放物品；
3. 预设分类初始化必须具备幂等性，重复执行不能生成重复数据；
4. 允许成员在登记物品时新建分类；
   - 新分类名称在提交前保持为可编辑草稿；
   - 提交时与物品和首条操作日志在同一事务中创建；
5. 分类名称去除首尾空格并规范化后不能重复；
6. 独立分类管理页面只对管理员和所有者开放；
7. 管理员和所有者可以重命名、停用和重新启用自定义分类；
8. 预设分类不能重命名、停用或删除；
9. 管理员和所有者可以删除从未被物品引用的自定义分类；
10. 已被任何物品引用的自定义分类不能物理删除，只能停用并保留历史物品引用；
11. 分类引用检查和删除必须在同一事务中完成。

### 7.3 物品登记任务

1. 创建 `items` 集合及查询索引；
2. 实现物品表单：
   - 图片最多两张；
   - 名称；
   - 详情；
   - 单件或多件模式；
   - 多件模式下的具体数量；
   - 预设或自定义分类；
   - 提交梗概（去除首尾空格后 1～250 个字符）；
3. 单件模式的数量固定为 `1`；
4. 多件模式的数量必须是大于 `0` 的整数；
5. 物品名称去除首尾空格后长度为 1～100 个字符；
6. 物品详情去除首尾空格后最多 2000 个字符；
7. 图片字段只能保存云文件 ID，最多 2 个；
8. 服务端生成物品 ID、标签编码、版本号和时间；
9. 写入 `registered_by`、`updated_by`，初始版本为 `1`；
10. 登记物品和首条操作日志在同一事务中完成。

### 7.4 图片任务

1. 在实现前重新核对微信当前图片选择、压缩、上传和云存储限制；
2. 客户端在上传前检查图片数量和实际文件大小；
3. 图片上传至云存储，数据库只保存云文件 ID；
4. 物品写入失败时不得让失败状态显示为登记成功；
5. 编辑替换图片时，在新版本成功写入后再处理不再引用的旧文件；
6. 列表使用适合列表展示的图片尺寸，详情页再加载完整图片。

登记页面与图片真机验收步骤见
[`docs/阶段2物品登记页面与图片验收.md`](docs/阶段2物品登记页面与图片验收.md)。

### 7.5 操作日志

创建物品时必须同时写入一条操作日志：

- 动作：`CREATE`；
- 操作人；
- 操作时间；
- 提交梗概；
- 物品版本 `1`。

日志不保存或展示字段修改的具体内容。

### 7.6 验收标准

- 预设分类只初始化一次；
- 成员可以新建分类并立即用于登记；
- 普通成员不能进入分类管理或修改物品分类；
- 管理员和所有者可以管理自定义分类和调整物品分类；
- 未被物品引用的自定义分类可以删除，被引用的分类只能停用；
- 单件、多件及数量校验符合规则；
- 最多上传两张图片；
- 新物品包含登记人、最后修改人、日期和版本；
- 创建成功必然存在对应日志；
- 失败请求不会产生半条物品记录或错误成功提示。

## 8. 阶段 3：物品查询、小程序码与实体标签

### 8.1 目标

用户可以通过文字、分类或实体小程序码快速定位物品。

### 8.2 文字与分类查询

1. 建立物品列表页；
2. 支持按名称和详情搜索；
3. 支持按分类筛选；
4. 默认只展示 `IN_STOCK` 物品；
5. 明确排序规则，默认按最后更新时间倒序；
6. 使用分页加载，避免一次读取全部物品；
7. 根据最终查询方式建立数据库索引；
8. 增加无结果、加载中、加载失败和无权限状态。

### 8.3 物品详情

详情页展示：

- 最多两张图片；
- 名称和详情；
- 数量模式及数量；
- 分类；
- 登记日期；
- 最后更新日期；
- 登记人；
- 最后修改人；
- 当前版本；
- 操作日志入口；
- 编辑入口；
- 离库申请入口。

实现顺序说明：先交付详情信息本身；操作日志、编辑和离库申请入口随阶段 4、阶段 5 的对应功能一起加入，
不提前展示无法使用的按钮。

### 8.4 小程序码

1. 标签使用随机、不可猜测且不包含业务信息的 `label_code`；
2. 服务端调用微信小程序码接口；
3. 小程序码参数只携带标签编码，不携带名称、数量等可变信息；
4. 扫码进入详情页后，服务端根据标签编码查询当前物品；
5. 小程序码图片保存至云存储；
6. `item_labels` 记录标签与物品的绑定关系和状态；
7. 已离库或无效标签返回明确状态，不跳转到其他物品；
8. 小程序码生成失败时允许管理员重新生成，但不能产生多个同时有效的绑定。

### 8.5 30 mm × 30 mm 标签排版

标签图的逻辑内容至少包含：

- 可扫描的小程序码；
- 必要的留白区；
- 可选的简短物品编号；
- 不依赖会频繁变化的名称和数量。

在 T50 Pro 适配前先输出统一的单色位图打印任务。当前按 8 dots/mm 建立 240 × 240 dots 的基准，
最终有效打印区域、二维码边长和留白必须用实机校准。

### 8.6 验收标准

- 名称和详情搜索返回正确物品；
- 分类筛选及分页无重复、无漏项；
- 扫描在库或离库申请中的标签能够打开对应物品详情；已离库标签只返回状态提示；
- 小程序码不暴露物品字段或用户信息；
- 已离库和作废标签不能显示为在库；
- 在多种手机尺寸上详情页和扫码流程可用。

## 9. 阶段 4：编辑、版本冲突与操作日志

### 9.1 目标

任何成员都能编辑允许字段，同时避免后提交者无提示覆盖其他人的修改。

### 9.2 可编辑范围

成员可以修改：

- 图片；
- 名称；
- 详情；
- 数量模式；
- 数量。

物品 ID、标签编码、登记人、登记日期、状态和历史日志不能通过普通编辑接口修改。

### 9.3 乐观锁实现

1. 用户打开编辑页时保存基准版本和基准字段；
2. 提交时发送 `expected_version` 和提交梗概；
3. 服务端在事务中判断当前版本是否等于 `expected_version`；
4. 版本一致时：
   - 更新允许字段；
   - 版本号加 `1`；
   - 更新 `updated_by` 和 `updated_at`；
   - 同事务写入操作日志；
5. 版本不一致时返回：
   - 最新物品；
   - 最新版本号；
   - 冲突错误码；
6. 客户端使用“打开时版本、自己的草稿、服务端最新版”做三方对比；
7. 只突出显示发生变化的字段；
8. 用户基于最新版确认内容并重新提交，系统不能自动覆盖。

### 9.4 提交梗概

- 新建和编辑必须填写；
- 去除首尾空格后长度为 1–250 个字符；
- 列表中展示梗概、操作人、时间、动作和版本；
- 日志不展示图片、名称、详情或数量的前后值；
- 日志只能读取，不能编辑或删除。

### 9.5 测试重点

- 两名成员同时打开同一版本；
- 第一人提交后版本加一；
- 第二人提交收到冲突而非成功；
- 对比页准确标出双方变化；
- 第二人基于最新版重新提交成功；
- 更新和日志写入必须同成同败；
- 提交梗概为空或过长时拒绝。

### 9.6 验收标准

- 不会出现无提示覆盖；
- 冲突页能清楚区分最新版与本地草稿；
- 每次成功修改只生成一条日志；
- 日志只包含规定的审计信息；
- 并发测试、事务测试和页面人工测试通过。

## 10. 阶段 5：离库申请与管理员操作

### 10.1 目标

成员不能直接删除物品，只能申请离库；管理员可以审批或直接离库。

### 10.2 成员流程

1. 在在库物品详情页提交离库申请；
2. 必须填写离库原因；
3. 同一物品存在待审核申请时，不允许重复申请；
4. 成员可以查看自己的申请状态；
5. 被拒绝后物品保持在库，并允许再次申请。

### 10.3 管理员流程

1. 查看全部待审核离库申请；
2. 查看物品当前信息、申请人、原因和申请时间；
3. 同意时在事务中：
   - 检查物品仍在库；
   - 将物品状态改为 `OFF_SHELF`；
   - 记录审核人和时间；
   - 写入离库操作日志；
   - 将标签状态改为 `VOID`；
4. 拒绝时记录审核人、审核意见和时间，物品保持在库；
5. 管理员可直接离库，但必须填写提交梗概或离库原因；
6. 普通物品查询页提供管理员权限角色的批量离库入口，客户端提交每件物品的 `item_id` 与 `expected_version`，服务端必须全部校验通过后再提交事务；
7. 独立的离库物品管理页提供管理员权限角色单件或批量重新入库入口，批量操作填写一次提交梗概，恢复离库前业务字段，版本号递增并为每件物品写入 `INBOUND` 日志；
8. 独立的离库物品管理页仅展示 `OFF_SHELF` 物品，管理员权限角色可在此删除物品及图片，保留操作日志、离库申请和 `VOID` 标签；
9. 删除后的标签不可复用，扫码只显示物品已删除或标签已失效，不展示详情。

### 10.4 并发和异常

- 两名管理员同时审核时，只允许一个请求改变最终状态；
- 审核期间物品已离库时，后续请求返回明确冲突；
- 已离库物品不能再次申请离库；
- 标签、物品、申请和日志状态必须保持一致。
- 批量离库任一物品版本冲突时，整批不产生状态、标签或日志变更；
- 批量删除包含在库或申请中的物品时，整批拒绝并保留原数据。
- 批量重新入库必须校验每件物品当前版本和 `VOID` 标签状态，任一冲突或状态不符时整批不得修改物品。

### 10.5 验收标准

- 成员无法直接调用离库接口；
- 管理员能够同意、拒绝和直接离库；
- 拒绝后可以重新申请；
- 已离库物品只出现在离库物品管理页及其详情，不出现在其他列表或页面；
- 扫描已离库标签只显示“物品已离库，请在离库物品管理中查看”，不展示物品详情；
- 普通物品查询页能够批量离库；
- 离库物品管理页只显示已离库物品，并能够删除物品及图片，日志、申请和 `VOID` 标签仍保留；
- 管理员能够在离库物品管理页单件或批量将已离库物品重新入库，标签恢复可用并为每件物品新增 `INBOUND` 日志；
- 删除后的标签显示物品已删除或标签已失效；
- 成员能够在申请中心查看自己的离库申请状态；
- 删除后的物品数据不可恢复；离库物品本身可以由管理员重新入库。

## 11. 阶段 6：管理员提醒

### 11.1 目标

管理员能够在对应审核模块及时看到新的成员申请和离库申请，不增加独立提醒列表。

### 11.2 站内提醒

1. “成员审核”模块直接查询待审核申请数量并显示角标；
2. “离库审核”模块直接查询待处理申请数量并显示角标；
3. 管理员首页只在对应管理入口显示数量，不增加“管理员提醒”卡片；
4. 处理申请后对应列表和数量立即更新；
5. 申请已经被其他管理员处理时，详情展示最终状态；
6. 已存在的 `notifications` 数据不再作为新的业务待办写入，保留至迁移清理阶段。

### 11.3 微信订阅消息

1. 在微信公众平台申请合适的订阅消息模板；
2. 保存模板 ID 到服务端环境配置；
3. 仅在用户明确授权后发送；
4. 消息内容只包含审核所需的最少信息；
5. 发送失败不改变申请创建或审核结果；
6. 记录发送结果用于排查，但不向普通用户展示内部错误。

订阅消息受微信平台模板、授权次数和发送规则约束，实施时必须以当时的官方规则为准重新查证。

### 11.4 验收标准

- 新成员申请只在“成员审核”模块出现待处理数量；
- 新离库申请只在“离库审核”模块出现待处理数量；
- 处理申请后页面不需要重新进入首页即可更新列表和数量；
- 订阅消息只在已授权且满足平台规则时发送，失败不会影响审核结果。

## 12. 阶段 7：权限、审核体验与扫码查询补齐

### 12.1 目标

补齐第六轮验收发现的权限层级、审核详情、模块内提醒、分类信息对齐和小程序内主动扫码查询，形成可供多人日常使用的管理闭环。

### 12.2 角色与成员申请

1. 将角色扩展为 `OWNER / MANAGER / ADMIN / MEMBER`，服务端统一实现 `OWNER > MANAGER > ADMIN > MEMBER` 权限层级；
2. 加入申请增加 `requested_role`，申请人可选择普通成员或管理员，不能直接申请实际管理者；
3. 成员审核列表显示申请目标角色；普通管理员只能审批普通成员，实际管理者/所有者才能审批管理员申请；
4. 新增成员管理接口和页面，支持查看成员、移除/停用普通成员；实际管理者/所有者可以任免管理员；
5. 新增实际管理者任免和传位接口，传位目标必须是已通过审核的管理员；实际管理者可以直接传位，不需要所有者审批；所有者仍可以任免实际管理者；
6. 角色变更、成员移除和传位必须在服务端事务中完成，且任何操作都不能让系统失去最后一名有效实际管理者；
7. “删除成员”使用 `DISABLED` 软删除，保留用户记录和审计关联，不做物理删除；
8. 为现有只有 `OWNER/ADMIN/MEMBER` 的数据提供一次性迁移/初始化步骤，由所有者选择首位实际管理者，不自动猜测人员。

### 12.3 离库审核与物品详情联动

1. 离库审核列表返回物品 ID，并提供进入物品详情的明确操作；
2. 物品详情服务在管理员/实际管理者/所有者查看待审核物品时返回申请人、原因、申请时间和申请 ID；
3. 物品详情页在 `OUTBOUND_PENDING` 状态显示“同意离库”“拒绝离库”，沿用现有审批接口和版本/状态校验；
4. 离库审核列表可进入待审核物品详情；离库物品管理列表可进入已离库物品详情，已离库物品不得出现在其他列表或页面；
5. 已离库物品详情页提供管理员权限角色的“重新入库”“删除物品”，批量恢复/批量删除继续保留在离库物品管理列表；其他状态没有删除入口；
6. 处理成功后详情页、审核列表和数量局部刷新，不能依赖用户手动重新编译或重新进入页面。

### 12.4 小程序内扫码查询

1. 新增 `pages/scan/index`，首页“扫码查询”进入该页面；
2. 页面调用微信扫码能力，读取小程序码返回的页面路径或 `scene`，复用已有标签解析接口；
3. 解析成功后统一跳转 `/pages/item-detail/index`，不在扫码页重复实现物品查询；
4. 处理用户取消、普通二维码/条形码、无效 `scene`、已离库和已删除标签等状态，并提供重试入口；已离库标签只显示状态提示，不进入物品详情；
5. 微信扫一扫深链直接打开详情时，详情页先静默完成登录和成员身份校验；
6. 物品详情页顶部左侧箭头使用 `navigateBack` 返回上一页，旁边的房子图标按钮使用 `reLaunch` 回到统一功能首页；
7. 补充 Android/iOS 真机扫码验收，验证首页入口、实体标签、自动登录和错误提示。

### 12.5 界面调整

1. 物品详情“物品信息”中的分类行与其他字段使用统一标签宽度、基线和内容区域，长分类名可换行但不挤压标签；
2. 审核列表的物品名称、编码、申请人和状态保持清晰，详情按钮与审核按钮分开布局；
3. 首页移除独立“管理员提醒”入口，成员审核和离库审核卡片显示各自数量。

### 12.6 数据与接口验收

新增或确认以下索引（均非唯一，除特别标注外）：

- `user_openid_unique`：`openid` 升序，唯一；
- `user_status_role_created_at`：`status` 升序、`role` 升序、`created_at` 降序，非唯一；
- `join_request_applicant_status`：`applicant_id` 升序、`status` 升序，非唯一；
- `join_request_status_role_created_at`：`status` 升序、`requested_role` 升序、`created_at` 降序，非唯一；
- `outbound_request_item_status_created_at`：`item_id` 升序、`status` 升序、`created_at` 降序，非唯一。

自动化测试至少覆盖：角色越权、管理员申请审批限制、实际管理者最后一名保护、传位并发、成员软删除、物品详情审批、离库物品详情管理、扫码路径解析和分类布局数据。

### 12.7 验收标准

- 申请人能选择普通成员或管理员，审核列表能明确区分；
- 普通管理员不能批准管理员申请，实际管理者/所有者可以；
- 管理员能够管理普通成员，实际管理者能够任免管理员，所有者能够任免实际管理者；
- 任何时刻至少有一名有效实际管理者；实际管理者可直接传位，无需所有者审批，且不产生中间空档；
- 离库审核列表能打开待审核物品详情，离库物品管理列表能打开已离库物品详情；已离库物品不出现在其他页面；
- 首页扫码查询入口能识别有效物品小程序码并进入详情，异常状态可重试；
- 小程序重新进入、扫码深链进入详情时不要求重复点击登录；详情页可以返回首页；
- 分类信息行在不同长度分类名称下保持对齐；
- 相关自动化检查和两种角色以上的人工验收通过。

## 13. 阶段 8：硕方 T50 Pro 真机打印

### 13.1 前置条件

- 已取得硕方提供的微信小程序 SDK、文档和示例；
- 已有 T50 Pro 实机；
- 已有 30 mm × 30 mm 标签纸；
- 已确认 SDK 的授权、打包和发布限制。

### 13.2 集成原则

- 厂商 API 只允许出现在
  `miniprogram/services/printer/adapters/supvan-t50-pro/`；
- 页面和标签业务只依赖 `PrinterAdapter`；
- 不修改通用打印任务去适配某个页面；
- 不添加“打印成功”的模拟兜底；
- SDK 错误必须转换为稳定的业务错误状态。

厂商 SDK 没有提供可识别的语义版本号，当前版本使用下载包 SHA256
`97A3D04A7AACC4F246311E7562B9C4946929A5CBAC42EB83962C09A14890419C`
固定。SDK 未附带明确的再分发许可证，且官方下载页提示包内包含序列号，因此 SDK 本体不提交到
公有仓库，通过 `npm run install:supvan-sdk` 安装到 Git 忽略目录。

### 13.3 开发任务

1. 核对 SDK 支持的蓝牙协议、小程序基础库版本和设备权限；
2. 实现设备发现；
3. 实现连接、断开和连接状态查询；
4. 使用厂商 `doPrintImage` 接口读取微信云存储解析后的 HTTPS 原始 PNG，并转换为 SDK 打印数据；
5. 实现份数、打印进度和完成回调；
6. 处理蓝牙关闭、连接超时、设备断开、缺纸、低电量和打印失败；
7. 页面提供重新连接和重新打印入口；
8. 记录打印任务状态，但不能把调用 SDK 成功等同于标签已贴到物品；
9. 校准二维码尺寸、纠错等级、留白和打印浓度；
10. 验证连续打印和重复打印不会改变标签绑定关系。

### 13.3.1 当前代码进度

- 已实现 SDK 本地安装、哈希校验和 Git 隔离；
- 已实现设备发现、停止搜索、连接、断开和状态查询；
- 已实现标签预览页打印份数、设备列表、连接状态和打印入口；
- 已固定 30 × 30 mm、间隙纸、3 mm 间隙、浓度 4、速度 30 mm/s 的首轮测试参数；
- 已将 SDK 文档中的 `101-135` 结果码映射为稳定业务错误；
- 已完成至少一台手机连接 T50 Pro、打印 30 mm × 30 mm 标签并扫码打开正确物品；双平台、异常恢复和连续 20 张验收尚未完成。

### 13.4 真机测试

- iOS 和 Android 各至少一台设备；
- 首次授权与拒绝授权；
- 蓝牙关闭后开启；
- 搜索多个附近设备；
- 打印中断；
- 缺纸后恢复；
- 同一标签打印一份及多份；
- 连续打印多张不同标签；
- 弱光、倾斜、轻微污损和不同距离扫码；
- 标签贴到物品后的实际扫码速度。

### 13.5 验收标准

- 能稳定发现并连接 T50 Pro；
- 30 mm × 30 mm 标签内容不裁切；
- 常用手机微信可以快速识别小程序码；
- 设备错误有明确提示且不会显示虚假成功；
- 重新打印不会创建新的物品或错误标签绑定；
- 硕方 SDK 更新影响被限制在适配目录内。

## 14. 阶段 9：系统验收与发布

### 14.1 自动化检查

每次合并前执行：

```bash
npm run check
npm run build:cloud
```

检查内容至少包括：

- 待提交仓库文件凭据扫描；
- ESLint；
- 小程序、云函数和测试 TypeScript 类型检查；
- 权限、输入、路由、并发、日志、离库和打印任务测试；
- 云函数编译。

### 14.2 人工验收角色

至少使用：

- 1 个所有者账号；
- 1 个管理员账号；
- 2 个成员账号；
- 1 个未加入或被拒绝账号。

两名成员用于真实验证并发编辑，两名管理权限账号用于验证并发审核。

### 14.3 核心回归流程

1. 新用户登录并申请加入；
2. 管理员收到提醒并审核；
3. 成员新建分类并登记带图片物品；
4. 通过文字和扫码找到物品；
5. 两名成员同时编辑并触发冲突；
6. 查看操作日志；
7. 成员申请离库；
8. 管理员拒绝后成员再次申请；
9. 管理员同意离库；
10. 扫描原标签确认只显示“物品已离库，请在离库物品管理中查看”；
11. 使用 T50 Pro 打印并扫描实体标签。

### 14.4 发布准备

1. 设置正式 AppID 和云环境；
2. 复核数据库和云存储权限；
3. 配置合法域名及微信平台所需资料；
4. 检查隐私政策和用户信息使用说明；
5. 清理测试账号和测试数据；
6. 建立数据备份与日志保留策略；
7. 构建并上传体验版；
8. 完成多账号真机验收；
9. 提交微信审核；
10. 审核通过后发布并观察首批真实操作。

### 14.5 最终完成标准

- 《产品需求与架构》中的 MVP 验收用例全部通过；
- 所有角色权限由服务端强制执行；
- 核心写操作具备事务或等价的一致性保障；
- 并发编辑不会无提示覆盖；
- 物品日志不可篡改且信息符合约定；
- 离库物品可由管理员重新入库，删除后的物品数据不可恢复且标签状态一致；
- 管理员能够收到两类申请提醒；
- 实体标签打印和扫码通过真机测试；
- 仓库、云环境和微信平台中均不存在泄露凭证；
- 本地检查和 GitHub CI 均通过。

## 15. 每阶段交付规范

每个阶段交付时必须说明：

1. 本阶段实现了哪些用户可见功能；
2. 修改了哪些数据结构和接口；
3. 执行了哪些自动化检查；
4. 完成了哪些人工或真机测试；
5. 哪些内容因为外部资料或设备尚未验证；
6. 是否存在数据库迁移、云函数部署或平台配置步骤；
7. 下一阶段开始前还需要项目所有者提供什么。

没有执行的检查必须明确标记为“未验证”，不得写成“通过”。

## 16. 推荐的近期执行批次

### 批次 A：环境与身份闭环

包含阶段 0 和阶段 1。完成后可以登录、申请加入和审核成员，是后续所有功能的权限基础。

开始条件：

- AppID；
- 云环境 ID；
- 组织名称；
- 首位所有者人选。

### 批次 B：物品最小闭环

包含阶段 2 和阶段 3。完成后可以登记、查看、搜索、扫码和识别物品。

开始条件：

- 批次 A 验收通过；
- 确认小程序基础视觉信息；
- 云存储可用。

### 批次 C：协作与离库闭环

包含阶段 4、阶段 5 和阶段 6。完成后可以安全地多人编辑、审计和审批离库。

开始条件：

- 至少两名成员测试账号；
- 至少两名具有审核权限的测试账号。

### 批次 D：权限、扫码与外部能力

包含阶段 7、阶段 8 和阶段 9；微信订阅消息仅作为阶段 6 的可选外部渠道。

开始条件：

- 硕方 SDK；
- T50 Pro 实机和标签纸；
- 微信发布所需资料。

## 17. 当前下一步

阶段 5 的成员申请、管理员审批、直接/批量离库、已离库数据删除及恢复流程和阶段 6 的首次提醒实现已完成代码实现，部署与人工验收以
[`docs/阶段5成员离库申请云环境配置.md`](docs/阶段5成员离库申请云环境配置.md) 和
[`docs/阶段5管理员离库处理云环境配置.md`](docs/阶段5管理员离库处理云环境配置.md) 为准；第六轮人工验收已完成，但提醒入口需要按本轮需求收敛到两个审核模块。

阶段 7 的角色层级、成员角色申请与软停用、实际管理者传位、审核详情联动、模块内待处理数量、内置扫码查询、已离库数据隔离和分类信息对齐已完成代码实现；服务端保留状态与角色边界，删除仅接受 `OFF_SHELF`，传位不需要所有者审批。现阶段进入阶段 7 的部署与人工验收，首位实际管理者仍由所有者在成员管理页手动任命。
阶段 8 已取得并接入硕方原生微信小程序 SDK，且已完成至少一台手机的真实连接、30 × 30 mm 标签打印和扫码回查；下一步补充双平台连接、异常恢复、识别距离和连续 20 张验收，具体步骤见
[`docs/阶段8硕方T50Pro打印验收.md`](docs/阶段8硕方T50Pro打印验收.md)。
微信订阅消息等待取得模板 ID 后再接入。开发阶段生成的码指向开发版，正式打印前必须切换为正式版并重新生成。
